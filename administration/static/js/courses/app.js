(function(angular){
    'use strict';

    var app = angular.module('courses', [
        'ngResource',
        'timtec-models',
        'directive.fixedBar',
        'django'
    ]);

})(window.angular);
