
(function (angular) {
    'use strict';

    // Declare app level module which depends on filters, and services
    angular.module('reports', [
        'reports.controllers',
        'reports.services',
        'timtec-models',
        'ui.bootstrap',
        'header']);
})(angular);
