
(function (angular) {
    'use strict';

    // Declare app level module which depends on filters, and services
    angular.module('reports', ['reports.controllers', 'reports.service', 'adminHeader']);
})(angular);
